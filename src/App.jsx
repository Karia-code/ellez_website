import React from 'react';

export default function App() {
  return (
    <div style={{ padding: 24 }}>
      <h1>ELLEZ Website</h1>
      <p>This is the React App placeholder. The static homepage remains served via index.html.</p>
      <p>Use /admin for the CMS panel once routing is wired to the HTML entry.</p>
    </div>
  );
}
