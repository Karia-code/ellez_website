{
  "name": "ellez-website-root",
  "private": true,
  "scripts": {
    "//說明": "--- 快速指令區 ---",
    "dev:admin": "cd admin-app && npm run dev",
    "build:admin": "cd admin-app && npm install && npm run build",
    "//說明2": "--- 部署指令區 ---",
    "predeploy": "npm run build:admin",
    "deploy": "npm run predeploy && npx firebase deploy",
    "//說明3": "--- 測試指令區 ---",
    "serve": "npx firebase emulators:start"
  },
  "devDependencies": {
    "firebase-tools": "^15.0.0"
  }
}
